#include "../../inc/bsb_engine/bsb_particelsystem.h"

Bsb_ParticelSystem::Bsb_ParticelSystem(){
	 
/*	for (int i=0; i<100; ++i) {
        particel.push_back(i); // Fügt i ans Ende von v an.
		//cout << particel[i];
	}*/ //funktioniert nocht nicht (bit)
}

void Bsb_ParticelSystem::update(float p_seconds){// Aufruf einmal pro Frame

}
void Bsb_ParticelSystem::render(){                    // Aufruf einmal pro Frame

}
void Bsb_ParticelSystem::set(){                      // … steht für Attribute des Objekts

}

