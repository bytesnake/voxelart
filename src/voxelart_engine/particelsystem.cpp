#include "../../inc/voxelart_engine/particelsystem.h"

vaEngine::ParticelSystem::ParticelSystem(){
	 
/*	for (int i=0; i<100; ++i) {
        particel.push_back(i); // Fügt i ans Ende von v an.
		//cout << particel[i];
	}*/ //funktioniert nocht nicht (bit)
}

void vaEngine::ParticelSystem::update(float p_seconds){// Aufruf einmal pro Frame

}
void vaEngine::ParticelSystem::render(){                    // Aufruf einmal pro Frame

}
void vaEngine::ParticelSystem::set(){                      // … steht für Attribute des Objekts

}

